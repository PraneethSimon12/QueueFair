# Booking service — Django + DRF + PostgreSQL. Python 3.12+.
# Direct dependencies only. Transitive deps (asgiref, sqlparse, tzdata, psycopg-binary,
# typing-extensions) are resolved by pip. If we ever need fully reproducible transitive pins,
# add pip-tools and a lock file — not needed at v0.
Django==5.1.15
djangorestframework==3.16.1
psycopg[binary]==3.3.4
PyJWT==2.13.0
